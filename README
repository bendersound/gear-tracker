To run this project
1. Install Apache Cordova
2. Open a terminal/command line in the root directory of the project
3. In cmd type, "cordova run android"

If an android device is plugged into the computer, the project will install
and run on that device. Otherwise, the project will run in any android emulators found.

No builds for other platforms are available at this time.

